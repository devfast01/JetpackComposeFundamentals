<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/mobile"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mobile"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackComposeFundamentals">
        <activity
            android:name=".BottomNavBarMulti.NavBarMultipleStackActivity"
            android:exported="false" />
        <activity
            android:name=".MultipleBackStacks1.MultiBackStack1Activity"
            android:exported="false" />
        <activity
            android:name=".PhilippLacknerNavigation.PhilippNavigationMainActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackComposeFundamentals">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>